apiVersion: v1
kind: Service
metadata:
  name: spring-boot-app-external-service
  namespace: argocd-operators
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80 
      protocol: TCP
      targetPort: 8080 
  selector:
    app: spring-boot-app
