apiVersion: v1
kind: Service
metadata:
  name: spring-boot-app-service
  namespace: argocd-operators
  labels:
    app: spring-boot-app 
spec:
  selector:
    app: spring-boot-app

  ports:
    - port: 8888
      targetPort: 8888

  type: LoadBalancer

